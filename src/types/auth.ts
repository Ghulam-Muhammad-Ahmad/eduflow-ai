export type AppRole = "teacher" | "student" | "admin";

/** Account type: business (owner), student, or tutor (tenant-created by owner). */
export type AccountType = "business" | "student" | "tutor";
